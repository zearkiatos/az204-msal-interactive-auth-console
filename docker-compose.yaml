services:
  console:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    working_dir: /app
    container_name: "az204-msal-interactive-auth-console"
    volumes:
      - .:/app
    tty: true